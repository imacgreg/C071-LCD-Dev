# Build output - regenerated by `make -f STM32Make.make` / the "Build STM" VSCode task
build/

# Local toolchain paths (ARM_GCC_PATH/OPENOCD) - specific to this machine's install locations
.stm32env

# OS junk
Thumbs.db
.DS_Store
